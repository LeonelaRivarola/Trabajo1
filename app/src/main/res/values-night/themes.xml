<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Trabajo1" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Barra de herramientas y elementos principales -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/on_primary_dark</item>

        <!-- Superficie de tarjetas y fondos -->
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/on_background_dark</item>

        <!-- Secundarios y de acento -->
        <item name="colorSecondary">@color/secondary_dark</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/on_secondary_dark</item>

        <!-- Barra de estado -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>
</resources>